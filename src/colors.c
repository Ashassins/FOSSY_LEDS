#include "colors.h"

color createColor (int rVal, int gVal, int bVal) {
    color newColor;
    newColor.r = rVal;
    newColor.g = gVal;
    newColor.b = bVal;
    return newColor;
}

void setColor (color value) {

}